<Window x:Class="Chess.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Chess.View"
        xmlns:locals="clr-namespace:Chess"
        xmlns:vm="clr-namespace:Chess.ViewModel"
        mc:Ignorable="d"
        Title="MainWindow" Height="800px" Width="800px" MinHeight= " 600" MinWidth="600" AllowsTransparency="True" WindowStartupLocation="CenterScreen" ResizeMode="CanResizeWithGrip" WindowStyle="None" WindowState="Normal">

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>
    
    

    <Window.Background>
        <SolidColorBrush Opacity="0.5" Color="Transparent"/>
    </Window.Background>

    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="/Pictures/chess2.jpg"/>
        </Grid.Background>
        <Grid  MouseDown=" Grid_MouseDown" HorizontalAlignment="Center" VerticalAlignment="Center" >


            <Frame x:Name="Rows" Width="auto" Height="auto" />
            
            

        </Grid>
    </Grid>
    <!--  <Frame x:Name="Rows"/>
        <Frame x:Name="testss"/>
        <Frame x:Name="Border"/> -->
  
</Window>